name: AWS RSS to Discord (Ultra Simple)

on:
  schedule:
    # รันทุก 5 นาที
    - cron: '*/5 * * * *'
  workflow_dispatch: # สามารถรันด้วยตนเองได้

jobs:
  check-rss:
    runs-on: ubuntu-latest
    
    steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: |
        npm init -y
        npm install rss-parser node-fetch@2
        
    - name: Check RSS and send to Discord
      run: |
        cat << 'EOF' > check-rss.js
        const Parser = require('rss-parser');
        const fetch = require('node-fetch');
        
        const parser = new Parser();
        const RSS_URL = 'https://aws.amazon.com/about-aws/whats-new/recent/feed/';
        const WEBHOOK_URL = process.env.DISCORD_WEBHOOK_URL;
        
        async function main() {
          try {
            console.log('🔍 Checking AWS RSS feed...');
            
            // อ่าน RSS feed
            const feed = await parser.parseURL(RSS_URL);
            console.log(`📰 Found ${feed.items.length} items in feed`);
            
            // กรองเฉพาะข่าวใน 3 นาทีล่าสุด (เข้มงวดมาก)
            const now = new Date();
            const cutoffTime = new Date(now.getTime() - 1440 * 60 * 1000); // 1440 นาทีที่แล้ว
            
            console.log(`⏰ Current time: ${now.toISOString()} (${now.toLocaleString('th-TH', {timeZone: 'Asia/Bangkok'})} Bangkok)`);
            console.log(`⏰ Looking for items newer than: ${cutoffTime.toISOString()} (${cutoffTime.toLocaleString('th-TH', {timeZone: 'Asia/Bangkok'})} Bangkok)`);
            
            const recentItems = feed.items.filter(item => {
              const itemDate = new Date(item.pubDate);
              const isRecent = itemDate >= cutoffTime;
              
              console.log(`📰 Item: ${item.title.substring(0, 50)}...`);
              console.log(`   📅 Published: ${itemDate.toISOString()} (${itemDate.toLocaleString('th-TH', {timeZone: 'Asia/Bangkok'})} Bangkok)`);
              
              if (isRecent) {
                const ageMinutes = Math.round((now - itemDate) / (1000 * 60));
                console.log(`   ✨ RECENT! Age: ${ageMinutes} minutes`);
              } else {
                const ageMinutes = Math.round((now - itemDate) / (1000 * 60));
                console.log(`   ⏰ Too old: ${ageMinutes} minutes`);
              }
              
              return isRecent;
            });
            
            console.log(`🆕 Found ${recentItems.length} items from last 3 minutes`);
            
            // ส่งรายการใหม่ทั้งหมดไปยัง Discord
            if (recentItems.length > 0) {
              console.log(`📤 Sending ${recentItems.length} new items to Discord...`);
              
              for (let i = 0; i < recentItems.length; i++) {
                const item = recentItems[i];
                await sendToDiscord(item, i + 1, recentItems.length);
                
                // รอ 2 วินาทีระหว่างการส่งเพื่อป้องกัน Discord rate limit
                if (i < recentItems.length - 1) {
                  await new Promise(resolve => setTimeout(resolve, 2000));
                }
              }
            } else {
              console.log('📭 No new items to send (no items in last 1440 minutes)');
            }
            
          } catch (error) {
            console.error('❌ Error:', error);
          }
        }
        
        async function sendToDiscord(item, index = 1, total = 1) {
          try {
            // สร้างข้อความสำหรับ Discord
            const embed = {
              title: item.title.length > 256 ? item.title.substring(0, 253) + '...' : item.title,
              url: item.link,
              description: item.contentSnippet ? 
                (item.contentSnippet.length > 500 ? 
                  item.contentSnippet.substring(0, 497) + '...' : 
                  item.contentSnippet) : 'No description available',
              color: 0xFF9900, // AWS orange color
              timestamp: item.isoDate || item.pubDate,
              footer: {
                text: `AWS What's New • ${index}/${total}`,
                icon_url: 'https://a0.awsstatic.com/libra-css/images/site/fav/favicon.ico'
              },
              fields: [
                {
                  name: '📅 Published',
                  value: new Date(item.pubDate).toLocaleDateString('th-TH', {
                    timeZone: 'Asia/Bangkok',
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit',
                    hour12: false
                  }) + ' (Bangkok)',
                  inline: true
                }
              ]
            };
            
            const payload = {
              username: 'AWS Updates',
              avatar_url: 'https://a0.awsstatic.com/libra-css/images/site/fav/favicon.ico',
              embeds: [embed]
            };
            
            const response = await fetch(WEBHOOK_URL, {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
              },
              body: JSON.stringify(payload)
            });
            
            if (response.ok) {
              console.log(`✅ Sent: ${item.title.substring(0, 50)}...`);
            } else {
              const errorText = await response.text();
              console.error(`❌ Failed to send to Discord: ${response.status} - ${errorText}`);
            }
            
          } catch (error) {
            console.error('❌ Error sending to Discord:', error);
          }
        }
        
        main();
        EOF
        
        node check-rss.js
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
